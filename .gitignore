# --- macOS junk ---
.DS_Store

# --- Xcode user/ephemeral files ---
xcuserdata/
*.xcuserstate
*.moved-aside
*.xccheckout
*.xcscmblueprint

# Ignore the per-user scheme management plist, keep shared schemes
**/xcschemes/xcschememanagement.plist

# --- Build artifacts ---
# --- macOS junk ---
.DS_Store

# --- Xcode user/ephemeral files ---
xcuserdata/
*.xcuserstate
*.moved-aside
*.xccheckout
*.xcscmblueprint

# Ignore per-user scheme management plist, keep shared schemes
**/xcschemes/xcschememanagement.plist

# --- Build artifacts ---
DerivedData/
build/
Build/
*.ipa
*.dSYM
*.dSYM.zip

# --- Swift Package Manager ---
.build/
.swiftpm/
!Package.resolved   # Keep lockfile committed

# --- CocoaPods (if added later) ---
Pods/

# --- Carthage (if added later) ---
Carthage/Build/

# --- Fastlane (if added later) ---
fastlane/report.xml
fastlane/screenshots
fastlane/test_output

